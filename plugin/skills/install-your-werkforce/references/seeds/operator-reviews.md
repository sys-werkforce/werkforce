# Operator reviews - every founder sign-off and send-back at the desk
<!-- LEDGER (append-only, the review-gap data source). One line per operator decision: "- YYYY-MM-DD [department] TASK-ID signed-off | sent-back: {operator's reasons verbatim}". Appended only when the founder speaks at the founders-desk; the operator-review queue page never writes here. Lines are never edited or deleted. -->
